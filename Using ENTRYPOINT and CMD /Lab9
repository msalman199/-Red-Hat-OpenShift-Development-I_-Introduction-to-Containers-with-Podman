 Using ENTRYPOINT and CMD in Containerfiles
Objectives
By the end of this lab, you will be able to:

Understand the difference between ENTRYPOINT and CMD instructions
Configure container startup processes using ENTRYPOINT and CMD
Test container behavior with different command combinations
Override default commands at runtime
Prerequisites
Podman or Docker installed (Podman recommended for OpenShift compatibility)
Basic understanding of Containerfile/Dockerfile syntax
Terminal or command-line access
Lab Setup
Create a new directory for the lab:
mkdir entrypoint-cmd-lab && cd entrypoint-cmd-lab
Task 1: Writing a Containerfile with ENTRYPOINT and CMD
Subtask 1.1: Create a basic Containerfile
Create a file named Containerfile with the following content:

# Base image
FROM registry.access.redhat.com/ubi9/ubi-minimal

# Set ENTRYPOINT to a shell script
ENTRYPOINT ["echo", "Entrypoint says:"]

# Set default CMD arguments
CMD ["Default CMD message"]
Explanation:

ENTRYPOINT defines the main executable that runs when the container starts
CMD provides default arguments that get passed to the ENTRYPOINT
Together they form the complete command: echo "Entrypoint says:" "Default CMD message"
Subtask 1.2: Build the container image
Build the image with Podman:

podman build -t entrypoint-demo .
Expected Output:

STEP 1/3: FROM registry.access.redhat.com/ubi9/ubi-minimal
STEP 2/3: ENTRYPOINT ["echo", "Entrypoint says:"]
STEP 3/3: CMD ["Default CMD message"]
COMMIT entrypoint-demo
Task 2: Testing Container Behavior
Subtask 2.1: Run with default ENTRYPOINT and CMD
podman run --rm entrypoint-demo
Expected Output:

Entrypoint says: Default CMD message
Subtask 2.2: Override CMD at runtime
podman run --rm entrypoint-demo "Custom message"
Expected Output:

Entrypoint says: Custom message
Key Concept: When you provide arguments after the image name, they replace the CMD instruction.

Task 3: Advanced ENTRYPOINT/CMD Combinations
Subtask 3.1: Create a script-based ENTRYPOINT
Create a new file greet.sh:

#!/bin/sh
echo "Welcome to $1 from $2"
Make it executable:

chmod +x greet.sh
Update your Containerfile:

FROM registry.access.redhat.com/ubi9/ubi-minimal

COPY greet.sh /usr/local/bin/
ENTRYPOINT ["/usr/local/bin/greet.sh"]
CMD ["OpenShift Lab", "Red Hat"]
Rebuild the image:

podman build -t greet-demo .
Subtask 3.2: Test the script-based container
podman run --rm greet-demo
Expected Output:

Welcome to OpenShift Lab from Red Hat
Override arguments:

podman run --rm greet-demo "Container Workshop" "Instructor"
Expected Output:

Welcome to Container Workshop from Instructor
Task 4: Overriding ENTRYPOINT
Subtask 4.1: Use --entrypoint flag
podman run --rm --entrypoint echo greet-demo "This completely replaces the ENTRYPOINT"
Expected Output:

This completely replaces the ENTRYPOINT
Troubleshooting Tip: If you get permission errors on the script, ensure it has execute permissions in the image and your host system.

Task 5: Understanding Shell vs Exec Form
Subtask 5.1: Create a shell-form example
Update Containerfile:

FROM registry.access.redhat.com/ubi9/ubi-minimal

# Shell form (note: not recommended for most cases)
ENTRYPOINT echo "Shell form ENTRYPOINT:"
CMD echo "Shell form CMD"
Key Difference:

Exec form (JSON array) doesn't invoke a shell
Shell form runs through /bin/sh -c
Conclusion
In this lab, you learned:

The difference between ENTRYPOINT and CMD instructions
How to combine ENTRYPOINT and CMD for flexible container startup
Methods to override both ENTRYPOINT and CMD at runtime
The importance of using exec form for proper signal handling
Best Practices:

Prefer exec form (JSON array) for ENTRYPOINT
Use CMD for default arguments that should be easily overridden
Combine ENTRYPOINT with a script for complex startup logic
Document expected runtime overrides in your image documentation
Cleanup
Remove all lab containers and images:

podman rmi entrypoint-demo greet-demo
Additional Exercises
Create a container that runs a Python script with ENTRYPOINT and accepts command-line arguments via CMD
Experiment with combining ENTRYPOINT, CMD, and environment variables
Research how OpenShift uses these directives in Deployments
